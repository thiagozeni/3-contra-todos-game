node_modules/
dist/
dist-beta/
**/_old/
**/dont-use/
/videos/
_excluded/

# macOS
.DS_Store
**/.DS_Store

# Scratch de extração de frames via ffmpeg (não é asset do app)
public/videos/loader-frames/

# Backups pesados em _artes-originais/ — ficam só local, nunca no git
_artes-originais/Prints/
_artes-originais/Screenshots App Store/
_artes-originais/Telas Game/
_artes-originais/audio-old/
_artes-originais/cenario-old/
_artes-originais/imgs_game_werdum/
_artes-originais/music-old/
_artes-originais/personagens-old/
_artes-originais/sfx-unused/
_artes-originais/sprites-source/
_artes-originais/sprites-werdum-old/

# Segurança — chaves, credenciais e secrets
.env
.env.local
.env.*.local
service_account.json
*.pem

# Android signing — NUNCA commitar keystores ou senhas
android/app/keystore/
android/keystore/
*.keystore
*.jks
android/gradle.properties
android/local.properties
keystore.properties

# verifier hook reports (local only)
.claude/verifier-reports/
.claude/.verifier-iter-*
.claude/.verifier-skip
coverage/

# E2E smoke screenshots (local artifacts only)
_e2e-shots/
.claude/smoke-v2/

# Fatia V — screenshots de trabalho (mockups HTML e refs ficam versionados)
docs/fatia-v/mockups/*.png

# Conceitos de arte do GPT (fontes pesadas, fora do repo)
_gpt_concept/
.secrets/
.claude/settings.local.json

# ── Scratch de exploração (local only, não versionar) ─────────────────
# Working dir de exploração de arte/IA (masters 4k, grids, insp-*, playtest) — ~100MB
_work/
# Scripts one-off de debug/screenshot (Playwright) — prefixo _; os já versionados
# continuam versionados. Novos ficam locais. Forçar com `git add -f` se algum virar tool.
scripts/_*.mjs
# Screenshots gerados (loja / auditoria) — artefatos pesados, regeráveis
scripts/_shots/
# Refs/masters de arte pesados da Fatia V (a arte REAL de produção vive em public/).
docs/fatia-v/art/*.png
docs/fatia-v/art/official-16x9/
docs/fatia-v/mockups/gpt-target/
public/imgs/cenario/game-bg-bleed.png
